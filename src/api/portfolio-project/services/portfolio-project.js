'use strict';

/**
 * portfolio-project service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::portfolio-project.portfolio-project');
